import { useState } from "react";import { useSuspenseQuery } from "@tanstack/react-query";import { ArrowUpRight, Boxes, Sparkles } from "lucide-react";import { productsQueryOptions, type Product } from "@/lib/products";import { Reveal } from "./reveal";import { WaitlistDialog } from "./waitlist-dialog";import { cn } from "@/lib/utils";function statusStyle(status: string) {  const s = status.toLowerCase();  if (s === "available") return "bg-cyan/15 text-cyan";  if (s === "beta") return "bg-royal/15 text-royal";  if (s === "in development") return "bg-violet/15 text-violet";  return "bg-accent text-accent-foreground";}function ProductCard({ product, index }: { product: Product; index: number }) {  const [waitlistOpen, setWaitlistOpen] = useState(false);  const url = product.website_url?.trim();  const href = url ? (/^https?:\/\//i.test(url) ? url : `https://${url}`) : null;  const logoUrl = product.logo_url?.includes("von.png") ? "/von.png" : product.logo_url?.includes("haku.png") ? "/haku.png" : product.logo_url?.includes("academy.png") ? "/academy.png" : product.logo_url?.includes("aevon.png") ? "/aevon.png" : product.logo_url;  return (    <Reveal as="article" delay={index * 70} className="h-full">      <div className="glass group relative flex h-full flex-col overflow-hidden rounded-[1.75rem] p-6 transition-all duration-500 hover:-translate-y-1.5 hover:glow-ring">        <div          aria-hidden          className="brand-gradient pointer-events-none absolute -right-16 -top-16 h-40 w-40 rounded-full opacity-0 blur-3xl transition-opacity duration-500 group-hover:opacity-40"        />        <div className="relative flex items-start justify-between gap-4">          <div className="glass grid h-14 w-14 shrink-0 place-items-center rounded-2xl">            {product.logo_url ? (              <img                src={logoUrl}                alt={`${product.name} logo`}                loading="lazy"                className="h-9 w-9 transition-transform duration-500 group-hover:scale-110"              />            ) : (              <Boxes className="h-6 w-6 text-primary" />            )}          </div>          <span            className={cn(              "rounded-full px-3 py-1 text-[11px] font-semibold",              statusStyle(product.status),            )}          >            {product.status}          </span>        </div>        <div className="relative mt-5 min-w-0 flex-1">          <p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-primary">            {product.category}          </p>          <h3 className="mt-1.5 text-xl font-semibold">{product.name}</h3>          <p className="mt-2.5 text-sm leading-relaxed text-muted-foreground">            {product.description}          </p>          {product.platform && (            <p className="mt-4 truncate text-xs text-muted-foreground">              <span className="font-medium text-foreground">Platform:</span> {product.platform}            </p>          )}          {product.tags.length > 0 && (            <ul className="mt-4 flex flex-wrap gap-1.5">              {product.tags.map((tag) => (
